<?xml version="1.0" encoding="utf-8"?>
<manifest package="mobi.carton"
          xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- permission -->

    <!-- required for the (GoogleGlass-alike) compass -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    -->

    <!-- required for the speech to text feature -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <!-- Activities (all in landscape orientation) -->

        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".compatible.CompatibleAppsActivity"
            android:screenOrientation="landscape">
        </activity>
        
        <activity
            android:name=".origami.OrigamiActivity"
            android:screenOrientation="landscape">
        </activity>

        <activity
            android:name=".origami.OrigamiStepsActivity"
            android:screenOrientation="landscape">
        </activity>

        <activity
            android:name=".tutorial.TutorialActivity"
            android:screenOrientation="landscape">
        </activity>

        <activity
            android:name=".tutorial.TutorialHeadActivity"
            android:screenOrientation="landscape">
        </activity>

        <activity
            android:name=".tutorial.TutorialSpeechActivity"
            android:screenOrientation="landscape">
        </activity>

    </application>

</manifest>
